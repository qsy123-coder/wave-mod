# 项目重构沉淀文档

## 项目概况
当前项目是「鸣潮角色 MOD 个人站」，技术栈为 Next.js 16.2.3 + React 19.2.4 + TypeScript 5.9 + Tailwind CSS 4 + Supabase + shadcn/ui。项目已使用 App Router、Server Components、Server Actions，并完成了初步组件目录重构：`src/app` 负责路由，`src/components/ui` 负责基础 UI，`src/components/layout` 负责布局，`src/components/common` 负责通用业务组件，`src/components/features` 负责业务特性组件，`src/actions` 负责 Server Actions。

## 重构目标
本轮长期优化目标：
1. 阶段 1：清理无用依赖与 Base UI 去耦，降低运行时依赖体积和维护成本。
2. 阶段 2：统一客户端数据获取策略，将 SWR 迁移到 TanStack Query。
3. 阶段 3：启用 React Compiler 与 Cache Components，完成 Suspense / PPR 基础适配。
4. 阶段 4：拆分公开详情数据与用户态详情状态，避免共享缓存污染用户状态。
5. 阶段 5：建立公开只读 Supabase 查询边界，让公开查询不依赖 request cookies。
6. 阶段 6：在低风险公开只读数据上试点 `"use cache"`。
7. 阶段 7：补充精细缓存标签与统一失效策略，接入 `cacheTag`、`cacheLife` 与公开 MOD 缓存刷新封装。
8. 阶段 8：统一错误处理与日志策略，收敛服务端 `console.error` 噪声并建立统一 logger。
9. 阶段 9：引入 Supabase 类型生成与类型安全改造，减少手写 Row 类型与数据库 schema 漂移风险。
10. 阶段 10：拆分 `src/lib/mods.ts`，按 types、mappers、public、viewer、admin、comments、sorting 等职责模块化。
11. 阶段 11：统一复杂后台表单策略，将上传/编辑表单迁移到 `react-hook-form + zod + Server Actions`。
12. 阶段 12：完善图片与性能优化策略，收窄远程图片域名、优化首屏 LCP 与预览图加载成本。
13. 阶段 13：引入测试体系，覆盖纯函数、组件和核心 E2E 路径。
14. 阶段 14：增加 CI/CD，自动执行 lint、typecheck、build 和测试脚本。
15. 阶段 15：OSS 上传能力整理，评估是否需要服务端 OSS 管理封装、孤儿文件清理和成本控制策略。
16. 阶段 16：邮箱登录中国化改造，将当前 Magic Link / OTP 体验升级为更适合中国用户的邮箱验证码登录体验。
17. 阶段 17：微信登录接入，优先建设中国用户主入口的网页登录方案。
18. 阶段 18：手机号验证码登录接入，提升移动端与普通用户登录转化。
19. 阶段 19：QQ 登录接入，作为游戏/年轻用户补充登录渠道。
20. 阶段 20：账号绑定与登录方式管理，统一微信、邮箱、手机号、QQ 的站内账号归属。
21. 阶段 21：国际化与文案治理评估；当前中文站点暂不优先，待有海外访问需求时再引入。
22. 每个阶段开始前必须先在本文档写入“当前阶段目标 / 阶段范围 / 非目标”，执行时严格遵守阶段边界。
23. 每个重要阶段完成后必须同步更新本文档和 `docs/技术栈分析.md`，并使用 Git Bash 执行验证。

## 当前阶段目标（阶段 18：手机号验证码登录接入）

### 阶段范围
本阶段只处理手机号验证码登录的可行性确认、登录入口设计和最小实现边界，目标是在不破坏现有邮箱登录、微信入口和 Supabase Auth 统一身份中心的前提下，为中国用户补齐手机号登录路径或明确短信服务限制。

### 目标
1. 查阅当前 Supabase Auth Phone OTP 文档，确认 `signInWithOtp({ phone })`、`verifyOtp()`、短信 Provider 与中国大陆短信送达的边界。
2. 梳理 `src/actions/auth/auth-actions.ts`、`src/app/auth/login/page.tsx`、`src/components/features/auth/login-form.tsx` 对手机号登录的扩展点。
3. 保持阶段 16 邮箱验证码登录作为稳定兜底，保留阶段 17 微信登录配置边界。
4. 若当前项目可直接使用 Supabase Phone OTP，则新增手机号发送验证码和验证流程的最小实现。
5. 若无法在本地无短信 Provider 配置情况下完成真实手机号登录，则新增清晰手机号登录入口、配置缺失提示和国内短信服务商接入清单，不伪造可用短信登录。
6. 明确手机号验证码登录的基础风控要求：手机号限流、IP 限流、验证码有效期、输错限制和防刷短信策略。
7. 阶段完成后必须执行 Git Bash 验证：`npm run lint && npx tsc --noEmit && npm run build && npm test`。
8. 阶段完成后同步更新 `docs/技术栈分析.md`、`docs/中国用户登录方案文档.md` 和本文档。

### 非目标
- 不接入 QQ 登录；QQ 登录留到阶段 19。
- 不新增账号绑定表或登录方式管理；账号绑定留到阶段 20。
- 不配置真实短信服务商密钥、短信签名或短信模板。
- 不自建完整短信网关，不引入新的认证后端。
- 不修改数据库 schema、RLS、公开页面缓存策略或用户数据结构。

## 重构记录（按时间倒序）

### 2026-05-06 - 【阶段 18 完成：手机号验证码登录接入】
**阶段目标回顾：**
- 本阶段严格限定为手机号验证码登录的可行性确认、登录入口设计和最小实现边界。
- 继续保留邮箱验证码登录作为稳定兜底，保留微信登录配置边界，未修改数据库 schema、RLS、Session 或用户数据结构。

**文档确认：**
- 已通过 Context7 查询 Supabase Auth Phone OTP 文档。
- Supabase Auth 支持 `signInWithOtp({ phone })` 发送短信验证码。
- Supabase Auth 支持 `verifyOtp({ phone, token, type: "sms" })` 验证 6 位短信验证码并创建会话。
- Supabase 文档提供了 Send SMS Hook 示例，可将短信发送桥接到 Twilio 等服务商；中国大陆送达率和短信签名模板需要后续结合国内短信服务商单独评估。

**修改内容：**
- 文件列表：
  - `src/actions/auth/auth-actions.ts`
  - `src/components/features/auth/login-form.tsx`
  - `docs/技术栈分析.md`
  - `docs/中国用户登录方案文档.md`
  - `沉淀文档.md`
- 主要变更点：
  - 新增手机号归一化逻辑，支持中国大陆 11 位手机号与 `+86` 格式。
  - 新增 `ENABLE_SUPABASE_PHONE_OTP` 开关，只有设置为 `true` 时才会真正调用 Supabase Phone OTP。
  - 新增 `sendPhoneOtp()` Server Action，用于发送短信验证码；默认未启用时返回明确配置提示。
  - 新增 `verifyPhoneOtp()` Server Action，用于验证 6 位短信验证码；验证成功后创建 Supabase 会话并跳转收藏页。
  - 登录页普通用户模式新增手机号验证码区域：手机号输入、发送短信验证码、验证码输入、验证并登录。
  - 手机号发送按钮新增 60 秒重新发送倒计时，避免重复点击。
  - 管理员模式不展示手机号登录入口，继续优先邮箱登录。

**改动原因：**
- 手机号验证码是中国用户非常熟悉的登录方式，但真实短信能力依赖短信服务商、短信签名、短信模板、资费与风控。
- 本地代码阶段不能替代短信服务商配置，因此通过 `ENABLE_SUPABASE_PHONE_OTP` 显式开关避免误触发不可控短信发送。
- 先建立 UI 入口、Server Action 边界和风控提示，有利于后续接入真实短信通道。

**影响范围：**
- 影响 `/auth/login` 普通用户模式登录页，新增手机号验证码登录区域。
- 默认未配置 `ENABLE_SUPABASE_PHONE_OTP=true` 时，不会真实发送短信，只会返回配置缺失提示。
- 不影响管理员登录、邮箱验证码登录、微信入口、登录回调、RLS、数据库结构或用户态数据。

**后续真实接入清单：**
- 在 Supabase Auth 中启用 Phone Provider。
- 配置短信服务商、短信签名、短信模板、验证码有效期和发送频率。
- 针对中国大陆用户评估默认短信通道送达率；如不满足，需接入国内短信服务商或 Supabase Send SMS Hook。
- 增加服务端/边缘层风控：手机号限流、IP 限流、验证码输错限制、冷却时间、黑名单和行为验证码。
- 阶段 20 账号绑定时统一处理手机号与邮箱、微信、QQ 的账号合并。

**验证结果：**
- Git Bash 已执行：`npm run lint && npx tsc --noEmit && npm run build && npm test`，结果通过。
- Vitest 仍为 3 个测试文件、15 个测试用例全部通过。
- 构建输出保持 `Cache Components enabled`，`/auth/login` 仍为 Partial Prerender 页面。

---

### 2026-05-06 - 【阶段 17 完成：微信登录接入】
**阶段目标回顾：**
- 本阶段严格限定为微信登录接入的可行性确认、登录入口设计和最小实现边界。
- 继续保留 Supabase Auth 作为统一身份中心，不修改数据库 schema、RLS、Session、邮箱登录兜底能力或用户数据结构。

**文档确认：**
- 已通过 Context7 查询 Supabase Auth 当前 OAuth 文档。
- Supabase Auth 官方外部 Provider 列表包含 Apple、Azure、Bitbucket、Discord、Facebook、Figma、GitHub、GitLab、Google、Keycloak、LinkedIn、Notion、Slack、Snapchat、Spotify、Twitch、Twitter、WorkOS 等，但未包含微信 Provider。
- 因此当前不能直接使用 `supabase.auth.signInWithOAuth({ provider: "wechat" })` 完成原生微信登录。

**修改内容：**
- 文件列表：
  - `src/actions/auth/auth-actions.ts`
  - `src/app/auth/login/page.tsx`
  - `src/components/features/auth/login-form.tsx`
  - `docs/技术栈分析.md`
  - `docs/中国用户登录方案文档.md`
  - `沉淀文档.md`
- 主要变更点：
  - 新增 `signInWithWechat()` Server Action，读取 `WECHAT_AUTH_BRIDGE_URL` 作为后续微信认证桥接地址。
  - 未配置 `WECHAT_AUTH_BRIDGE_URL` 时，微信登录会回到当前登录页并显示“微信登录尚未配置”的明确错误提示。
  - 已为后续桥接服务预留 `next`、`mode`、`callback_url` 参数。
  - 登录页普通用户模式新增“微信快捷登录”按钮；管理员模式不展示微信登录入口，继续优先邮箱登录。
  - 登录页补充微信登录需要微信开放平台网站应用和认证桥接地址的提示。

**改动原因：**
- 微信登录是中国用户体验最好的主入口之一，但本地代码阶段无法替代微信开放平台资质、AppID、AppSecret 和生产回调域名。
- 在 Supabase Auth 当前无原生微信 Provider 的情况下，本阶段不能伪造可用微信 OAuth，只能建立清晰入口、配置边界和后续桥接协议。
- 这样既能让 UI 和阶段计划前进，也避免用户点击后进入不可解释的失败状态。

**影响范围：**
- 影响 `/auth/login` 普通用户模式登录页，新增微信快捷登录入口。
- 未配置 `WECHAT_AUTH_BRIDGE_URL` 时不会离开站点外部服务，只会返回登录页提示配置缺失。
- 不影响管理员登录、邮箱验证码登录、登录回调、RLS、数据库结构或用户态数据。

**后续真实接入清单：**
- 申请微信开放平台网站应用。
- 准备备案域名和微信回调域名。
- 实现微信 OAuth 桥接服务，处理授权跳转、state 校验、code 换 token、openid/unionid 获取、用户映射和失败回跳。
- 设计桥接服务与 Supabase Auth 的会话打通方式。
- 阶段 20 账号绑定时统一处理微信、邮箱、手机号、QQ 的账号合并。

**验证结果：**
- Git Bash 已执行：`npm run lint && npx tsc --noEmit && npm run build && npm test`，结果通过。
- Vitest 仍为 3 个测试文件、15 个测试用例全部通过。
- 构建输出保持 `Cache Components enabled`，`/auth/login` 仍为 Partial Prerender 页面。

---

### 2026-05-05 - 【阶段 16 完成：邮箱登录中国化改造】
**阶段目标回顾：**
- 本阶段严格限定为现有邮箱登录体验的中国化改造。
- 继续保留 Supabase Auth 作为统一身份中心，未接入微信、手机号或 QQ 登录，未修改数据库 schema、RLS、Session、登录回调或用户数据结构。

**修改内容：**
- 文件列表：
  - `src/actions/auth/auth-actions.ts`
  - `src/app/auth/login/page.tsx`
  - `src/components/features/auth/login-form.tsx`
  - `docs/技术栈分析.md`
  - `docs/中国用户登录方案文档.md`
  - `沉淀文档.md`
- 主要变更点：
  - 登录页标题和主文案从“登录链接 / Magic Link”心智调整为“邮箱验证码登录”优先。
  - 增加支持邮箱提示：QQ 邮箱、163、126、Outlook、Gmail、企业邮箱。
  - 登录表单新增发送成功后的操作说明，提示查看邮箱、检查垃圾邮件箱和必要时更换邮箱重试。
  - 登录表单新增 60 秒重新发送倒计时，降低重复点击和邮件发送频率。
  - 管理员登录说明调整为邮箱稳定登录优先，并继续强调只有 `ADMIN_EMAIL` 指定账号可进入后台。
  - 登录回调缺少授权码的错误文案调整为“缺少验证码授权码，请重新发送邮箱验证码”。
  - Server Action 成功文案调整为“邮箱验证码登录邮件已发送，请前往邮箱查收”。

**改动原因：**
- 中国用户对邮箱验证码的理解成本通常低于 Magic Link。
- 当前阶段不应推翻 Supabase Auth，因此先在现有 `signInWithOtp()` 链路上完成页面体验和中文文案改造。
- 发送成功反馈、重新发送倒计时和收不到邮件提示能提升 QQ/网易/Outlook/Gmail 等邮箱场景下的可用性。

**影响范围：**
- 影响 `/auth/login` 页面 UI 文案与交互状态。
- 不影响登录回调、会话创建、管理员校验、普通用户 profile 创建、RLS 或数据库结构。
- 不新增第三方 Provider，也不修改 Supabase 邮件模板或邮件服务商配置。

**后续注意事项：**
- 当前仍使用 Supabase `signInWithOtp()` 与邮件跳转回调链路，页面表达为“邮箱验证码登录”。
- 如果后续需要真正的站内 6 位验证码输入框，需要继续评估 Supabase OTP 验证模式、邮件模板、`verifyOtp()` 流程和邮件送达服务商。
- 阶段 17 将进入微信登录接入，需先确认微信开放平台资质、回调域名和是否通过 Supabase Provider 或自定义桥接实现。

**验证结果：**
- Git Bash 已执行：`npm run lint && npx tsc --noEmit && npm run build && npm test`，结果通过。
- Vitest 仍为 3 个测试文件、15 个测试用例全部通过。
- 构建输出保持 `Cache Components enabled`，`/auth/login` 仍为 Partial Prerender 页面。

---

### 2026-05-05 - 【阶段 15 完成：OSS 上传能力整理】
**阶段目标回顾：**
- 本阶段严格限定为阿里云 OSS 上传链路的边界梳理、共享工具整理和后续治理策略记录。
- 未改为服务端中转上传，未改用 Supabase Storage 存储大文件，未改变数据库 schema、RLS、页面 UI、Server Action 语义、缓存策略或公开下载行为。

**修改内容：**
- 文件列表：
  - `src/lib/oss/shared.ts`
  - `src/lib/oss/server-config.ts`
  - `src/app/api/oss/sign/route.ts`
  - `src/components/features/admin/upload/oss-image-upload.tsx`
  - `src/components/features/admin/upload/oss-zip-upload.tsx`
  - `src/lib/oss/shared.test.ts`
  - `docs/技术栈分析.md`
  - `沉淀文档.md`
- 主要变更点：
  - 新增 `src/lib/oss/shared.ts`，统一 OSS 共享常量和工具：上传方法、Policy 过期时间、图片/ZIP Content-Type 白名单、图片 20MB / ZIP 2GB 上限、公开 URL、上传 URL、文件名清洗和体积展示。
  - `src/lib/oss/server-config.ts` 改为复用 `normalizeOssEndpoint()`，统一 endpoint 规范化逻辑。
  - `src/app/api/oss/sign/route.ts` 改为复用共享 OSS 工具，不再在路由内重复维护文件名清洗、公开 URL 和上传 URL 生成逻辑。
  - 签名接口新增 `fileSize` 参数校验，并根据图片/ZIP 分类执行服务端 Content-Type 白名单校验与体积上限校验。
  - OSS Policy 的 `content-length-range` 由统一 2GB 收窄为按类型控制：图片 20MB、ZIP 2GB。
  - 图片上传组件和 ZIP 上传组件改为复用共享常量生成 `accept`、展示体积上限、执行客户端预校验，并统一走 OSS POST 表单直传。
  - 新增 `src/lib/oss/shared.test.ts`，覆盖 endpoint 规范化、URL 构造、文件名清洗、上传类型识别和体积上限格式化。

**改动原因：**
- OSS 上传约束原本分散在签名接口和前端上传组件中，文件大小、Content-Type 和 URL 规则容易漂移。
- 抽出共享 OSS 工具后，服务端签名与客户端上传可复用同一套约束，降低维护成本和隐式耦合。
- 先建立体积限制和分类校验，有助于控制误传文件、上传失败排查和后续成本治理。

**影响范围：**
- 影响后台 OSS 图片/ZIP 上传链路和签名接口的校验规则。
- 不改变已成功上传文件的公开 URL 结构，仍保持 `mods/{character}/{modId}/{filename}` 路径模式。
- 不改变 MOD 表单字段结构和公开下载行为。

**孤儿文件 / 成本控制结论：**
- 当前项目尚未记录 objectKey 元数据，无法精确识别“上传成功但表单未提交”的 OSS 孤儿文件。
- 后续如需精确清理，建议新增文件对象元数据表或定时审计任务，记录 objectKey、用途、关联 MOD、上传状态和最终提交状态。
- Bucket 侧可后续补充生命周期规则、存储类型转换、访问日志和流量告警；本阶段只记录策略，不接入真实删除/扫描 API。

**验证结果：**
- Git Bash 已执行：`npm run lint && npx tsc --noEmit && npm run build && npm test`，结果通过。
- Vitest 现为 3 个测试文件、15 个测试用例全部通过。
- 构建输出保持 `Cache Components enabled`，首页 `/` 仍为 `Revalidate 1m / Expire 1h`。

---

### 2026-05-05 - 【阶段 14 完成：增加 CI/CD】
**阶段目标回顾：**
- 本阶段严格限定为最小 CI 质量检查工作流。
- 未接入自动部署或发布流程，未配置生产密钥、Supabase 密钥或 OSS 密钥，未改变测试覆盖范围或业务源码行为。

**修改内容：**
- 文件列表：
  - `.github/workflows/quality.yml`
  - `docs/技术栈分析.md`
  - `沉淀文档.md`
- 主要变更点：
  - 新增 GitHub Actions 工作流 `Quality Checks`。
  - 工作流触发范围覆盖 `push` 和 `pull_request`。
  - 使用 `actions/checkout@v4` 拉取代码。
  - 使用 `actions/setup-node@v4` 安装 Node.js 22，并启用 npm 缓存。
  - 使用 `npm ci` 安装锁定依赖。
  - CI 自动执行 `npm run lint`、`npx tsc --noEmit`、`npm run build`、`npm test`。
  - 添加 `concurrency`，同一分支新提交会取消旧的未完成质量检查。

**改动原因：**
- 阶段 13 已具备 `npm test`，阶段 14 将本地质量命令串入 CI，避免只依赖手动验证。
- 最小质量检查工作流能在 Pull Request 或推送时尽早发现 lint、类型、构建和测试问题。
- 当前项目尚未绑定部署平台，因此本阶段不加入自动部署，以免扩大密钥和发布风险。

**影响范围：**
- 新增 CI 配置，不改变运行时业务行为。
- 后续在 GitHub 上推送或创建 Pull Request 时会自动运行质量检查。
- CI 构建如未来需要访问真实 Supabase / OSS，可再通过 GitHub Secrets 单独配置；本阶段不写入任何密钥。

**验证结果：**
- 工作流文件 linter 检查无报错。
- Git Bash 已执行：`npm run lint && npx tsc --noEmit && npm run build && npm test`，结果通过。
- 验证期间曾因上一轮被中断的 `next build` 仍在运行而触发一次构建锁提示；等待原验证完成后确认最终命令退出码为 0。
- 构建输出保持 `Cache Components enabled`，首页 `/` 仍为 `Revalidate 1m / Expire 1h`。

---

### 2026-05-05 - 【阶段 13 完成：引入测试体系】
**阶段目标回顾：**
- 本阶段严格限定为测试体系的最小可用接入与第一批低风险测试用例。
- 未接入 CI/CD，未强制引入组件测试或 E2E 测试矩阵，未修改数据库 schema、RLS、页面 UI、缓存策略或 Server Action 语义。

**修改内容：**
- 文件列表：
  - `package.json`
  - `package-lock.json`
  - `vitest.config.ts`
  - `src/lib/mods-domain/sorting.test.ts`
  - `src/lib/admin/mod-form.test.ts`
  - `docs/技术栈分析.md`
  - `沉淀文档.md`
- 主要变更点：
  - 新增 `vitest@4.0.7` 开发依赖。
  - 新增 `npm test` 脚本，执行 `vitest run`。
  - 新增 `vitest.config.ts`，配置 Node 测试环境、`@` 路径别名和 `src/**/*.{test,spec}.{ts,tsx}` 测试匹配范围。
  - 新增 `src/lib/mods-domain/sorting.test.ts`，覆盖排序参数解析、角色/关键词过滤解析、收藏排序、热度计算与查询过滤。
  - 新增 `src/lib/admin/mod-form.test.ts`，覆盖后台 MOD 创建/编辑 schema、字段错误转换、FormData 解析、图片 URL 拆分和标签拆分。
  - 本阶段评估后暂不接入 React Testing Library 与 Playwright，避免引入 jsdom / 浏览器下载 / 端到端环境配置导致阶段范围过大。

**改动原因：**
- 当前项目已有较多纯函数和 Zod schema，但此前缺少可重复执行的测试命令。
- 阶段 13 先覆盖无需真实 Supabase、OSS 或浏览器环境的纯逻辑，可以低风险建立测试基线。
- 将组件测试和 E2E 测试留到后续分阶段接入，有利于控制依赖体积、配置复杂度和维护成本。

**影响范围：**
- 新增测试与测试配置，不改变运行时业务行为。
- `npm test` 现在可作为本地质量检查命令，也可在阶段 14 CI/CD 中直接复用。
- 组件测试和 E2E 测试仍未接入，后续可继续扩展。

**验证结果：**
- Git Bash 已执行：`npm test`，结果通过：2 个测试文件、11 个测试用例全部通过。
- Git Bash 已执行：`npm run lint && npx tsc --noEmit && npm run build && npm test`，结果通过。
- 构建输出保持 `Cache Components enabled`，首页 `/` 仍为 `Revalidate 1m / Expire 1h`。

---

### 2026-05-04 - 【阶段 12 完成：图片与性能优化策略】
**阶段目标回顾：**
- 本阶段严格限定为图片域名边界、关键页面图片加载优先级和低风险图片性能优化。
- 未改变数据库 schema、OSS 上传流程、图片字段结构、页面业务语义、缓存策略或整体 UI 风格。

**修改内容：**
- 文件列表：
  - `next.config.ts`
  - `src/components/features/home/hero-carousel.tsx`
  - `src/components/features/mods/detail/mod-preview-gallery.tsx`
  - `src/components/common/mod-card.tsx`
  - `src/components/features/mods/list/mods-infinite-grid.tsx`
  - `src/components/features/admin/upload/upload-form.tsx`
  - `src/components/features/mods/list/mods-react-query-demo.tsx`
  - `docs/技术栈分析.md`
  - `沉淀文档.md`
- 主要变更点：
  - `next.config.ts` 移除允许任意 `http` / `https` 远程图片的宽泛配置。
  - 当前 `next/image` 远程来源收敛为：`images.unsplash.com`、示例 OSS 域名、`ALIYUN_OSS_BUCKET + ALIYUN_OSS_ENDPOINT` 对应 Bucket 域名，以及 `**.aliyuncs.com` OSS 域名兜底。
  - 首页 hero 轮播主图从原生 `<img>` 迁移到 `next/image`，首张图设置 `priority`、`fetchPriority="high"` 和明确 `sizes`。
  - 详情页主预览图和缩略图从原生 `<img>` 迁移到 `next/image`，主预览图设置高优先级加载，缩略图设置响应式 `sizes`。
  - `ModCard` 增加 `imageFetchPriority` 参数；MOD 列表首屏前 4 张卡片设置 `imagePriority`、`imageFetchPriority="high"` 和更准确的 `sizes`。
  - 后台上传表单预览图继续保留原生 `<img>`，原因是该区域预览用户刚上传/粘贴的任意图片 URL，避免受到 `next/image` 远程域名白名单影响；本阶段补充 `loading="lazy"` 与 `decoding="async"`。
  - React Query 示例中的原生 `<img>` 补充 `loading="lazy"` 与 `decoding="async"`，降低非关键示例区图片解码成本。

**改动原因：**
- 允许所有远程图片域名会扩大生产安全边界，也会让不可控来源进入 `next/image` 优化链路。
- 首页 hero 和详情主预览属于用户首先看到的大图，适合使用 `next/image` 的尺寸、优先级和优化能力。
- 后台预览图更偏向编辑辅助，不属于公开首屏关键路径，保留原生 `<img>` 更符合任意 URL 预览需求。

**影响范围：**
- 影响 `next/image` 可加载的远程图片来源；后续生产 MOD 预览图建议使用阿里云 OSS 或已纳入白名单的可信域名。
- 不改变 MOD 图片字段结构，不改变 OSS 上传流程。
- 不改变页面布局和业务交互。

**验证结果：**
- Git Bash 已执行：`npm run lint && npx tsc --noEmit && npm run build`，结果通过。
- 构建输出保持 `Cache Components enabled`，首页 `/` 仍为 `Revalidate 1m / Expire 1h`。

---

### 2026-05-04 - 【阶段 11 完成：统一复杂后台表单策略】
**阶段目标回顾：**
- 本阶段严格限定为后台 MOD 上传/编辑表单的表单状态、字段校验和提交体验统一。
- 未改变数据库 schema、RLS、缓存策略、日志策略、MOD 数据展示结构、公开页面交互或后台 UI 风格。

**修改内容：**
- 文件列表：
  - `src/lib/admin/mod-form.ts`
  - `src/actions/admin/upload-actions.ts`
  - `src/actions/admin/edit-mod-actions.ts`
  - `src/components/features/admin/upload/upload-form.tsx`
  - `docs/技术栈分析.md`
  - `沉淀文档.md`
- 主要变更点：
  - 新增 `src/lib/admin/mod-form.ts`，统一后台 MOD 表单 schema、初始 state、字段错误转换、FormData 解析、图片 URL 拆分和标签拆分逻辑。
  - 上传 Action 从本地重复 schema 改为复用 `adminModFormSchema`、`parseAdminModFormData()`、`splitImageUrls()`、`splitTags()`。
  - 编辑 Action 从本地重复 schema 改为复用 `adminModUpdateFormSchema`、`parseAdminModUpdateFormData()`、`splitImageUrls()`、`splitTags()`。
  - 共用 `UploadForm` 接入 `react-hook-form + zodResolver`，创建和编辑模式共享同一套客户端校验与服务端兜底校验。
  - 保留 `useActionState` + Server Actions 提交链路，避免改变现有服务端写入、权限校验和 revalidate 语义。
  - OSS ZIP 上传、OSS 图片上传、删除预览图、NSFW 勾选、角色 datalist、XXMI 安装说明等交互均保持原功能。
  - 使用 `useWatch()` 订阅 `downloadUrl` 与 `imageUrls`，避免 React Compiler 对 React Hook Form `watch()` 给出 `react-hooks/incompatible-library` warning。

**改动原因：**
- 上传和编辑 Action 原本各自维护一份几乎相同的 Zod schema、字段解析和错误转换逻辑，后续新增字段时容易遗漏。
- 后台 MOD 表单属于复杂表单，包含 OSS 上传回填、图片预览、字段校验、编辑默认值和服务端写入，适合由 React Hook Form 管理客户端状态。
- 同时保留 Server Actions 兜底校验，确保绕过客户端校验时仍不会写入非法数据。

**影响范围：**
- 影响后台 `/admin/upload` 与 `/admin/mods/[id]/edit` 共用表单。
- 不改变管理员权限保护、OSS 上传签名、Supabase 写入字段、公开页面数据结构或 API 返回结构。
- `react-hook-form` 和 `@hookform/resolvers` 现在已正式成为实际使用依赖。

**验证结果：**
- Git Bash 已执行：`npm run lint && npx tsc --noEmit && npm run build`，结果通过。
- 首次验证发现 React Hook Form `watch()` 触发 React Compiler 兼容 warning，已改用 `useWatch()` 后重新验证通过且无 warning。
- 构建输出保持 `Cache Components enabled`，首页 `/` 仍为 `Revalidate 1m / Expire 1h`。

---

### 2026-05-04 - 【阶段 10 完成：拆分 `src/lib/mods.ts` 职责模块】
**阶段目标回顾：**
- 本阶段严格限定为 `src/lib/mods.ts` 的职责拆分与兼容导出。
- 未改变业务语义、数据库 schema、RLS、API 返回结构、页面 UI、缓存策略、日志策略或表单策略。

**修改内容：**
- 文件列表：
  - `src/lib/mods.ts`
  - `src/lib/mods-domain/types.ts`
  - `src/lib/mods-domain/mappers.ts`
  - `src/lib/mods-domain/sorting.ts`
  - `src/lib/mods-domain/public.ts`
  - `src/lib/mods-domain/viewer.ts`
  - `src/lib/mods-domain/detail.ts`
  - `src/lib/mods-domain/favorites.ts`
  - `src/lib/mods-domain/comments.ts`
  - `src/lib/mods-domain/admin.ts`
  - `docs/技术栈分析.md`
  - `沉淀文档.md`
- 主要变更点：
  - 新建 `src/lib/mods-domain/`，避免 Windows 下 `mods.ts` 与同名 `mods/` 目录冲突。
  - 将业务展示类型与数据库 Row 辅助类型拆到 `types.ts`。
  - 将 `mapMod()`、`mapComment()` 和 `publicModColumns` 拆到 `mappers.ts`。
  - 将排序、筛选、解析、校验和错误判断工具拆到 `sorting.ts`。
  - 将公开只读查询拆到 `public.ts`，包括角色、列表、分页、首页榜单和详情基础数据。
  - 将用户态收藏/点赞/评分状态拆到 `viewer.ts`。
  - 将公开详情组合函数拆到 `detail.ts`。
  - 将收藏页数据拆到 `favorites.ts`。
  - 将评论分页数据拆到 `comments.ts`。
  - 将后台 MOD 列表数据拆到 `admin.ts`。
  - `src/lib/mods.ts` 保留为兼容聚合导出入口，现有页面、组件、API Route、Server Action 导入路径无需批量修改。

**改动原因：**
- 原 `src/lib/mods.ts` 同时承担类型、映射、排序、公开查询、用户态查询、收藏、评论和后台查询，单文件职责过重。
- 阶段 10 拆分后，每类逻辑有明确归属，后续表单迁移、测试补充或查询优化时可以更小范围修改。
- 保留兼容导出入口可以降低一次性重构风险，避免影响现有调用方。

**影响范围：**
- 对外仍可从 `@/lib/mods` 获取原有导出，调用语义保持不变。
- 公开缓存、用户态读取、后台读取、评论分页、收藏页读取逻辑保持原行为。
- 不改变 UI 和 API 返回结构。

**验证结果：**
- Git Bash 已执行：`npm run lint && npx tsc --noEmit && npm run build`，结果通过。
- 构建输出保持 `Cache Components enabled`，首页 `/` 仍为 `Revalidate 1m / Expire 1h`。

---

### 2026-05-04 - 【阶段 9 完成：Supabase 类型生成与类型安全改造】
**阶段目标回顾：**
- 本阶段严格限定为 Supabase 数据库类型建立与小范围类型接入。
- 未拆分 `src/lib/mods.ts` 文件结构，未迁移表单，未引入测试或 CI/CD，未调整缓存策略、数据库 schema、RLS、UI 或 API 返回结构。

**修改内容：**
- 文件列表：
  - `src/types/supabase.ts`
  - `src/lib/supabase/server.ts`
  - `src/lib/supabase/admin.ts`
  - `src/lib/supabase/client.ts`
  - `src/lib/mods.ts`
  - `docs/技术栈分析.md`
  - `沉淀文档.md`
- 主要变更点：
  - 新增 `src/types/supabase.ts`，根据本地 `supabase/*.sql` schema 建立 `Database` 类型。
  - `Database` 覆盖当前核心表：`profiles`、`mods`、`favorites`、`likes`、`comments`、`ratings`。
  - 补充 `Tables`、`TablesInsert`、`TablesUpdate` 辅助类型，便于后续按表复用 Row/Insert/Update 类型。
  - 将 `createPublicReadClient()`、服务端 `createClient()`、`createAdminClient()`、浏览器 Supabase client 接入 `Database` 泛型。
  - 在 `src/lib/mods.ts` 中小范围复用 `Tables<...>`，让 `ModRow`、`FavoriteRow`、`CommentRow` 的基础字段开始来自统一数据库类型。
  - 保留 `SiteMod`、`AdminMod`、`ModComment` 等业务展示类型，避免数据库 Row 类型直接泄漏到组件层。

**改动原因：**
- 阶段 8 之前 MOD 查询相关 Row 类型主要手写维护，随着字段增多容易与数据库 schema 漂移。
- 先建立统一 `Database` 类型与 typed Supabase client，可以在不改变业务语义的前提下提升查询、写入和字段重构时的类型安全。
- 本阶段只做小范围接入，为阶段 10 拆分 `src/lib/mods.ts` 提供类型基础。

**注意事项：**
- `npx supabase --version` 曾触发临时安装，但没有可靠返回版本；本阶段未依赖远端 Supabase 项目或登录态，而是按仓库内 `supabase/*.sql` 建立本地类型文件。
- 后续如正式接入 Supabase CLI 生成类型，可用 CLI 输出替换或校准 `src/types/supabase.ts`。
- 当前 `Database` 类型已包含 `downloads_count`，该字段来自 `supabase/add_downloads.sql`。

**影响范围：**
- 不改变页面 UI、API 返回结构、Server Action 返回结构、数据库 schema、RLS 或缓存策略。
- Supabase 客户端现在具备数据库泛型约束，后续 `.from(...)`、`.insert(...)`、`.update(...)` 将获得更明确的类型反馈。
- `src/lib/mods.ts` 仍保持原文件结构，拆分留到阶段 10。

**验证结果：**
- Git Bash 已执行：`npm run lint && npx tsc --noEmit && npm run build`，结果通过。
- 构建输出保持 `Cache Components enabled`，首页 `/` 仍为 `Revalidate 1m / Expire 1h`。

---

### 2026-05-04 - 【阶段 8 完成：统一错误处理与日志策略】
**阶段目标回顾：**
- 本阶段严格限定为服务端错误输出与日志分级治理。
- 未做 Supabase 类型生成、`src/lib/mods.ts` 大拆分、表单迁移、测试体系、CI/CD、缓存策略继续调整或 UI 改动。

**修改内容：**
- 文件列表：
  - `src/lib/logger.ts`
  - `src/lib/mods.ts`
  - `src/actions/auth/auth-actions.ts`
  - `docs/技术栈分析.md`
  - `沉淀文档.md`
- 主要变更点：
  - 新增 `src/lib/logger.ts`，统一 `debug`、`info`、`warn`、`error` 输出入口。
  - `debug` / `info` 仅在非生产环境输出，`warn` / `error` 保持输出。
  - 将 `src/lib/mods.ts` 中公开读取、用户态状态读取、收藏页读取、评论分页读取的可降级错误改为 `logger.warn`。
  - 保留后台管理数据读取失败为 `logger.error`，因为这类问题影响后台数据可用性或服务端配置。
  - 将认证 Server Action 中直接 `console.log/error` 迁移到 `logger.info/error`，不改变登录邮件发送和错误返回语义。
  - 全项目业务代码中直接 `console.*` 已收敛到统一 logger 内部。

**改动原因：**
- 开发环境中直接 `console.error` 容易被 Next.js 以红色错误形式展示，部分可降级查询失败会干扰真实问题定位。
- 阶段 8 通过统一 logger 和日志分级，让公开读取 fallback、用户态状态读取失败等场景使用 `warn`，真正影响认证、后台、配置或数据一致性的错误保留 `error`。
- 后续如接入生产日志平台，只需扩展 `src/lib/logger.ts`，避免分散修改业务代码。

**影响范围：**
- 不改变页面 UI、API 返回结构、Server Action 返回结构、数据库 schema 或缓存策略。
- 不改变 Supabase 查询本身，仅改变日志输出入口与级别。
- 公开查询失败仍按原逻辑返回 fallback 或空数据。
- 登录失败仍按原逻辑返回错误信息。

**验证结果：**
- Git Bash 已执行：`npm run lint && npx tsc --noEmit && npm run build`，结果通过。
- 构建输出保持 `Cache Components enabled`，首页 `/` 仍为 `Revalidate 1m / Expire 1h`。

---

### 2026-05-04 - 【阶段 7 完成：精细缓存标签与统一失效策略】
**修改内容：**
- 文件列表：
  - `src/lib/mod-cache.ts`
  - `src/lib/mods.ts`
  - `src/actions/admin/upload-actions.ts`
  - `src/actions/admin/edit-mod-actions.ts`
  - `src/actions/admin/mod-actions.ts`
  - `src/actions/mods/favorite-actions.ts`
  - `src/actions/mods/like-actions.ts`
  - `src/actions/mods/rating-actions.ts`
  - `src/actions/mods/comment-actions.ts`
  - `docs/技术栈分析.md`
  - `沉淀文档.md`
- 主要变更点：
  - 新增 `src/lib/mod-cache.ts`，统一公开 MOD 缓存标签：`mods:characters`、`mods:list`、`mods:detail:{id}`。
  - 新增 `revalidatePublicModCaches(modId?)`，统一刷新公开 MOD 标签与关键路径：`/`、`/mods`、`/mods/{id}`。
  - 在 `getAvailableCharacters()` 中加入 `cacheTag(modCacheTags.characters)` 与 `cacheLife("hours")`。
  - 在 `getPublicMods()` 中加入 `cacheTag(modCacheTags.list)` 与 `cacheLife("minutes")`。
  - 在 `getPublicModBaseById()` 中按合法 MOD ID 加入 `cacheTag(modCacheTags.detail(id))` 与 `cacheLife("minutes")`。
  - 后台上传、编辑、删除、发布，以及收藏、点赞、评分、评论等会改变公开聚合字段的操作，已接入统一公开 MOD 缓存失效。
  - 当前 Next.js 16.2.3 的 `revalidateTag()` 需要第二个 profile 参数，本阶段使用 `"default"`。

**改动原因：**
- 阶段 6 已完成 `"use cache"` 试点，但只依赖默认缓存策略，缺少可控的标签化失效。
- 公开列表、角色标签、详情基础信息会被后台内容变化和互动聚合字段影响，需要统一失效入口。
- 将标签和路径刷新集中到 `revalidatePublicModCaches()`，避免每个 Action 手写分散的刷新逻辑。

**影响范围：**
- 影响公开首页榜单、角色标签、MOD 列表、MOD 详情基础数据缓存。
- 后台上传/编辑/删除/发布后会统一刷新公开缓存。
- 收藏、点赞、评分、评论后会刷新相关公开聚合缓存和收藏页路径。
- 评论分页本身仍不进入共享缓存；用户态字段仍不进入共享缓存。
- 浏览数、下载数 API 暂未接入统一缓存失效，避免高频请求造成缓存频繁失效；后续可单独评估更短缓存或实时计数接口。

**验证结果：**
- Git Bash 已执行：`npm run lint && npx tsc --noEmit && npm run build`，结果通过。
- 构建输出显示首页 `/` 当前为静态路由，缓存策略变为 `Revalidate 1m / Expire 1h`，符合列表/详情使用 `cacheLife("minutes")` 后的预期。

---

### 2026-05-04 - 【终端报错修复：开发期告警收敛】
**修改内容：**
- 文件列表：
  - `src/app/layout.tsx`
  - `src/components/common/mod-card.tsx`
  - `src/lib/mods.ts`
  - `沉淀文档.md`
- 主要变更点：
  - 给根 `<html>` 增加 `data-scroll-behavior="smooth"`，消除 Next.js 对 `scroll-behavior: smooth` 的开发期提示。
  - 给 `ModCard` 中 `next/image` 的 `fill` 父容器增加 `min-h-72`，避免开发期偶发父容器高度为 0 的图片告警。
  - 为 `getModCommentsPage()` 增加 AbortError 降噪逻辑：路由切换导致的 Supabase 查询中止不再用 `console.error` 打红。
  - 确认此前终端中的 `/` 404 与 hydration mismatch 为 dev 启动初期旧日志；新开发服务中 `/` 与 `/mods` 均返回 200，未再出现同类新报错。

**验证结果：**
- Git Bash 已执行：`npm run lint && npx tsc --noEmit && npm run build`，结果通过。
- 新启动开发服务后请求：
  - `/` 返回 200。
  - `/mods` 返回 200。
  - 新终端未再出现旧的 hydration、404、scroll-behavior 或 AbortError 告警。

---

### 2026-05-04 - 【阶段 6 完成：公开只读数据 use cache 试点】
**修改内容：**
- 文件列表：
  - `src/lib/mods.ts`
  - `docs/技术栈分析.md`
  - `沉淀文档.md`
- 主要变更点：
  - 在低风险公开只读函数中加入 `"use cache"` 指令：
    - `getAvailableCharacters()`
    - `getPublicMods()`
    - `getPublicModBaseById()`
  - 暂未缓存 `getModCommentsPage()`，避免评论新增/删除后的分页实时性问题。
  - 暂未缓存 `getViewerModState()`、`getFavoriteMods()`、后台认证和后台管理数据，避免用户态缓存污染。
  - 未引入 `cacheTag` / `cacheLife`，先采用 Next.js 16 默认缓存策略进行低风险试点。
  - 构建输出显示首页 `/` 已输出为静态路由，并显示默认 `Revalidate 15m / Expire 1y`。

**改动原因：**
- 阶段 4 已拆分公开详情基础数据与用户态详情状态。
- 阶段 5 已将公开查询切换到不读取 cookies 的 `createPublicReadClient()`。
- 因此阶段 6 可以在低风险公开数据上试点 `"use cache"`，验证 Next.js 16 Cache Components 与 Supabase 公开只读查询的兼容性。
- 先不引入 tag/life 是为了降低 API 兼容风险，确保基础缓存指令在当前 Next.js 16.2.3 项目中稳定构建。

**影响范围：**
- 影响公开角色标签、首页榜单、MOD 列表、MOD 详情基础信息的服务端公开数据读取缓存。
- 不影响用户收藏、点赞、评分状态读取。
- 不影响评论分页的实时读取。
- 不影响后台认证、后台管理、Server Actions、API Route 或 OSS 上传逻辑。

**注意事项 / 待验证点：**
- Git Bash 已执行完整验证：`npm run lint && npx tsc --noEmit && npm run build`，结果通过。
- 构建输出显示 `Cache Components enabled`，首页 `/` 带默认 `Revalidate 15m / Expire 1y`。
- 后台上传、编辑、发布、删除当前仍通过 `revalidatePath()` 刷新相关页面；后续阶段可补充更精细的 cache tag/life 与统一失效工具。
- 浏览器手动验证建议：检查首页榜单、角色标签、MOD 列表、MOD 详情基础信息是否正常；登录后确认收藏/点赞/评分状态仍按用户实时显示。

---

### 2026-05-04 - 【阶段 5 完成：建立公开只读 Supabase 查询边界】
**修改内容：**
- 文件列表：
  - `src/lib/supabase/server.ts`
  - `src/lib/mods.ts`
  - `docs/技术栈分析.md`
  - `沉淀文档.md`
- 主要变更点：
  - 在 `src/lib/supabase/server.ts` 新增 `createPublicReadClient()`。
  - `createPublicReadClient()` 使用 Supabase anon key 和 `@supabase/supabase-js` 创建服务端公开只读客户端，不读取 Next.js `cookies()`，也不持久化 auth session。
  - 将 `getAvailableCharacters()` 切换到 `createPublicReadClient()`，避免公开角色标签查询依赖 request cookies。
  - 将 `getPublicMods()` 切换到 `createPublicReadClient()`，使首页榜单、公开 MOD 列表和公开分页的基础数据源不依赖 request cookies。
  - 将 `getPublicModBaseById()` 切换到 `createPublicReadClient()`，延续阶段 4 的公开详情基础数据边界。
  - 将 `getModCommentsPage()` 切换到 `createPublicReadClient()`，使公开评论分页读取不依赖 request cookies。
  - 保持 `getViewerModState()`、`getFavoriteMods()` 等用户态函数继续使用 `createClient()`，确保它们仍读取当前用户 session。

**改动原因：**
- 阶段 4 已经拆分详情页公开基础数据和用户态状态，但公开查询仍通过 `createClient()` 创建 Supabase SSR 客户端，隐式读取 `cookies()`。
- 后续要对公开数据使用 `"use cache"`，公开查询不仅不能混入用户态字段，也应避免隐式依赖 request cookies。
- 新增公开只读客户端后，公开列表/榜单/角色/详情基础/评论分页具备更清晰的缓存候选边界。

**影响范围：**
- 影响公开读取路径：首页榜单、首页角色标签、MOD 列表、MOD 详情基础信息、评论分页。
- 不影响登录态收藏、点赞、评分、收藏页、后台认证和后台管理数据读取。
- 不改变 API Route、Server Actions、数据库 schema 或 OSS 上传逻辑。
- 依赖 Supabase RLS 中公开读取策略继续允许 anon key 读取已发布 MOD 和公开评论。

**注意事项 / 待验证点：**
- Git Bash 已执行完整验证：`npm run lint`、`npx tsc --noEmit`、`npm run build`，结果通过。
- 第一次完整验证时遇到 Next 构建锁提示，原因是已有 Next/Node 进程占用构建；随后分步执行 lint/tsc 与 build，最终构建通过。
- 本阶段仍未正式添加 `"use cache"`，只是让公开查询具备更安全的缓存前置条件。
- 浏览器手动验证建议：检查首页、MOD 列表、MOD 详情、评论区是否仍能读取公开数据；登录后检查收藏/点赞/评分状态是否仍实时显示。

---

### 2026-05-04 - 【阶段 4 完成：拆分公开详情数据与用户态详情状态】
**修改内容：**
- 文件列表：
  - `src/lib/mods.ts`
  - `src/app/(site)/mods/[id]/page.tsx`
  - `docs/技术栈分析.md`
  - `沉淀文档.md`
- 主要变更点：
  - 新增 `ViewerModState` 类型，明确当前用户视角的详情状态字段：`isFavorited`、`isLiked`、`userRating`。
  - 新增 `getPublicModBaseById(id)`：只读取公开 MOD 基础详情，不读取当前用户、不读取 cookies 相关状态。
  - 新增 `getViewerModState(modId)`：只读取当前登录用户对指定 MOD 的收藏、点赞、评分状态。
  - 保留兼容函数 `getPublicModById(id)`，内部组合公开基础详情和用户态状态，避免其他潜在调用点断裂。
  - 调整 MOD 详情页：从原来直接调用 `getPublicModById(id)`，改为并行调用 `getPublicModBaseById(id)`、`getViewerModState(id)`、`getCurrentUser()`、评论、管理员状态和推荐 MOD。
  - 同步更新 `docs/技术栈分析.md`，记录公开数据与用户态数据拆分进度。

**改动原因：**
- 阶段 3 已启用 Cache Components，下一步如果直接对详情数据使用 `"use cache"`，必须确保共享缓存中不包含用户态字段。
- 原 `getPublicModById()` 同时读取公开 MOD 数据和当前用户收藏/点赞/评分状态，不适合作为共享缓存边界。
- 本阶段先完成最关键的详情页数据边界拆分，为后续把公开基础详情接入 `"use cache"` 打基础，同时保证用户态状态仍按请求实时读取。

**影响范围：**
- 主要影响 `/mods/[id]` 详情页数据组合方式。
- 不改变详情页 UI、评论、收藏、点赞、评分、下载逻辑。
- 不改变 API Route、Server Actions、数据库 schema 或 OSS 上传逻辑。
- `getPublicModById()` 仍保留，降低重构风险。

**注意事项 / 待验证点：**
- Git Bash 已执行完整验证：`npm run lint && npx tsc --noEmit && npm run build`，结果通过。
- 本阶段仍未正式添加 `"use cache"`，只是完成缓存安全前置拆分。
- 下一阶段应继续拆分 `getPublicMods()`、`getAvailableCharacters()`、首页榜单函数等公开列表/聚合查询，避免它们隐式依赖 request cookies 后再接入缓存。
- 浏览器手动验证建议：打开任意 MOD 详情页，确认登录/未登录状态下收藏、点赞、评分状态显示正确。

---

### 2026-05-04 - 【阶段 3 完成：启用 React Compiler 与 Cache Components】
**修改内容：**
- 文件列表：
  - `next.config.ts`
  - `package.json`
  - `package-lock.json`
  - `src/app/(site)/page.tsx`
  - `src/app/(site)/mods/page.tsx`
  - `src/app/(site)/mods/[id]/page.tsx`
  - `src/app/(site)/favorites/page.tsx`
  - `src/app/admin/layout.tsx`
  - `src/app/admin/mods/page.tsx`
  - `src/app/admin/mods/[id]/edit/page.tsx`
  - `src/app/admin/upload/page.tsx`
  - `src/app/auth/login/page.tsx`
  - `src/lib/mods.ts`
  - `docs/技术栈分析.md`
  - `沉淀文档.md`
- 主要变更点：
  - 在 `next.config.ts` 启用 `reactCompiler: true` 与 `cacheComponents: true`。
  - 安装 React Compiler 所需 devDependency：`babel-plugin-react-compiler`。
  - 启用 Cache Components 后，构建暴露出多个动态数据读取未置于 Suspense 内的问题，本阶段逐一修复。
  - 首页：将角色分类数据读取下沉到 `HomeCharacterSection` 并用 Suspense 包裹；首页榜单和轮播继续保持 Suspense 边界。
  - MOD 列表页：将 `searchParams`、角色列表和首屏分页数据读取下沉到 `ModsPageContent` / `ModsFeed`，顶层页面只保留外壳与 Suspense。
  - MOD 详情页：将 `params` 的 await 移入 `ModDetailContent` Suspense 边界内。
  - 收藏页：将认证与收藏数据读取下沉到 `FavoritesContent`，顶层保留静态标题外壳和 Suspense。
  - 后台布局：将 `getCurrentUser()` 读取下沉到 `AdminUserEmail`，并用 Suspense 包裹，避免后台 layout 阻塞 PPR。
  - 后台列表、上传、编辑页：将认证和数据读取放入 Suspense 内部内容组件。
  - 登录页：将 `searchParams` 读取下沉到 `LoginPageContent`，并使用 Suspense 包裹。
  - `getWeeklyHotMods()` 移除构建期 `Date.now()`，改为基于已查询 MOD 的最新 `createdAt` 派生 7 天热度窗口。
  - 使用 `next build --debug-prerender` 定位 Cache Components 下的阻塞点，最终生产构建输出多个 Partial Prerender 页面。

**改动原因：**
- Next.js 16 的 Cache Components 会要求未缓存动态数据读取位于 Suspense 边界内，否则会阻塞整页渲染并导致构建失败。
- React Compiler 需要显式安装 `babel-plugin-react-compiler`，否则 Next.js 构建无法解析 React Compiler。
- 本阶段通过 Suspense/PPR 方式保留页面 shell 的快速预渲染能力，把动态 Supabase、cookies、searchParams、params 数据以服务端流式内容补齐。
- 移除 Server Component 中的构建期 `Date.now()`，避免时间依赖导致预渲染不可复现。

**影响范围：**
- 公开页面：`/`、`/mods`、`/mods/[id]`、`/favorites`、`/auth/login` 从构建输出看已进入 Partial Prerender 或静态/动态混合模式。
- 后台页面：`/admin/mods`、`/admin/upload`、`/admin/mods/[id]/edit` 也通过 Suspense 边界适配 Cache Components。
- 不改变 Supabase 查询语义、API Route、Server Actions、OSS 上传逻辑或数据库 schema。
- `getWeeklyHotMods()` 的 7 天窗口由“当前真实时间”改为“最新公开 MOD 时间向前 7 天”，用于首页榜单展示，避免构建期当前时间依赖。

**注意事项 / 待验证点：**
- Git Bash 已执行完整验证：`npm run lint && npx tsc --noEmit && npm run build`，结果通过。
- 构建输出显示 `Cache Components enabled`，并出现 `◐ (Partial Prerender)` 路由。
- 本阶段尚未为 `src/lib/mods.ts` 数据函数添加 `"use cache"`；下一阶段应拆分公开数据查询和用户态查询后再精细缓存。
- 浏览器手动验证建议：检查首页、MOD 列表筛选、MOD 详情、收藏页、登录页、后台上传/列表/编辑的加载骨架与数据内容是否按预期显示。
- npm audit 仍提示 3 个 moderate vulnerabilities，延续前序待处理项。

---

### 2026-05-04 - 【阶段 2 完成：统一客户端数据获取到 TanStack Query】
**修改内容：**
- 文件列表：
  - `src/components/features/mods/list/mods-infinite-grid.tsx`
  - `package.json`
  - `package-lock.json`
  - `docs/技术栈分析.md`
  - `沉淀文档.md`
- 主要变更点：
  - 将 MOD 列表无限加载从 `useSWRInfinite` 迁移到 TanStack Query 的 `useInfiniteQuery`。
  - 保持 `/api/mods?page=&pageSize=&sort=` 接口不变。
  - 保持 `initialMods` 作为 Server Components 首屏数据，并通过 `initialData` 注入 TanStack Query，避免首屏重复请求。
  - IntersectionObserver 无限滚动逻辑保持不变，但触发方式从 `setSize(size + 1)` 改为 `fetchNextPage()`。
  - 使用 `hasNextPage` 和 `isFetchingNextPage` 替代 SWR 的 `hasMore` 派生状态和 `isValidating`。
  - 使用 Git Bash 卸载 `swr`，完成客户端分页缓存策略统一。
  - 同步更新 `docs/技术栈分析.md`，标记 SWR 已移除，客户端数据层统一到 TanStack Query。

**改动原因：**
- 当前项目此前同时使用 SWR 和 TanStack Query，增加了客户端数据层心智负担和缓存策略复杂度。
- 评论区已经使用 TanStack Query 的 `useInfiniteQuery`，列表页也迁移到同一策略后，后续收藏、点赞、评论、列表刷新、乐观更新可以共享同一 QueryClient 和统一失效策略。
- 这符合 Next.js 16 项目常见最佳实践：首屏数据由 Server Components 获取，客户端交互分页由 TanStack Query 接管，避免重复数据库查询和不一致的缓存抽象。

**影响范围：**
- 主要影响 `/mods` 页面中的 MOD 无限列表加载。
- 不影响首页、详情页、后台、认证、评论区、点赞、收藏、下载接口。
- 不改变 API Route 返回结构，也不改变 `ModsInfiniteGrid` 对外 props。

**注意事项 / 待验证点：**
- Git Bash 已执行完整验证：`npm run lint && npx tsc --noEmit && npm run build`，结果通过。
- 后续浏览器手动验证建议：进入 `/mods`，滚动到底部，确认下一页 MOD 自动加载，筛选角色/关键词/排序切换后列表正常重置。
- `npm uninstall` 后仍提示 3 个 moderate vulnerabilities，延续阶段 1 待处理项。

---

### 2026-05-04 - 【阶段 1 完成：清理无用依赖与 Base UI 去耦】
**修改内容：**
- 文件列表：
  - `package.json`
  - `package-lock.json`
  - `docs/技术栈分析.md`
  - `沉淀文档.md`
  - `src/components/ui/avatar.tsx`
  - `src/components/ui/badge.tsx`
  - `src/components/ui/button.tsx`
  - `src/components/ui/dialog.tsx`
  - `src/components/ui/dropdown-menu.tsx`
  - `src/components/ui/input.tsx`
  - `src/components/ui/separator.tsx`
  - `src/components/ui/sheet.tsx`
  - `src/components/ui/tabs.tsx`
  - `src/types/ali-oss.d.ts`（已删除）
- 主要变更点：
  - 使用 Git Bash 卸载未实际需要的运行时依赖：`@base-ui/react`、`next-themes`、`proxy-agent`、`ali-oss`。
  - 删除旧的 `src/types/ali-oss.d.ts`，因为当前 OSS 上传采用 POST Policy + 浏览器直传，不再依赖 `ali-oss` 包类型。
  - 验证发现 `shadcn` 的 `base-nova` 风格基础 UI 组件实际引用了 `@base-ui/react` 子模块。
  - 为严格移除 `@base-ui/react`，将 `button`、`badge`、`avatar`、`input`、`separator`、`dialog`、`sheet`、`dropdown-menu`、`tabs` 改为本地 React 轻量实现，保留原导出名称，尽量维持现有调用方式不变。
  - 同步更新 `docs/技术栈分析.md`：标记阶段 1 已移除依赖，说明 `react-hook-form` 与 `@hookform/resolvers` 暂保留用于后续复杂表单迁移，`swr` 留到阶段 2 迁移后移除。

**改动原因：**
- 清理未使用依赖是长期架构治理的低风险起点，可降低依赖体积、安全审计面和升级维护成本。
- `@base-ui/react` 原本被基础 UI 组件间接依赖，如果只卸载包会导致 TypeScript 编译失败。因此本阶段同步完成 Base UI 去耦，改为项目本地可控的基础组件实现，符合“依赖最小化”和“UI 基础设施可控”的优化方向。
- 对 Next.js 16 项目而言，减少不必要运行时依赖有利于构建稳定性和长期升级。

**影响范围：**
- 受影响的基础 UI 组件：`Button`、`Badge`、`Avatar`、`Input`、`Separator`、`Dialog`、`Sheet`、`DropdownMenu`、`Tabs`。
- 当前业务页面主要使用 `Button`、`Badge`、`Input` 等组件；`Dialog`、`Sheet`、`DropdownMenu`、`Tabs` 当前项目中未发现业务引用，但保留导出以维持组件库完整性。
- 不影响 Supabase、Server Actions、路由、数据库 schema、OSS 上传签名逻辑。

**注意事项 / 待验证点：**
- 本地轻量版 `Dialog`、`Sheet`、`DropdownMenu`、`Tabs` 不是 Base UI 的完整无障碍等价实现；当前业务未实际使用这些高级组件，因此风险较低。如后续重度使用，建议按实际交互需求增强键盘导航、焦点管理和 ARIA 行为。
- Git Bash 已执行完整验证：`npm run lint && npx tsc --noEmit && npm run build`，结果通过。
- `npm uninstall` 后审计提示仍有 3 个 moderate vulnerabilities，未在本阶段处理，后续可单独执行 `npm audit` 评估是否需要修复。

---

### 下一步计划：
1. 阶段 19：QQ 登录接入，作为游戏/年轻用户补充登录渠道。
2. 执行前先在本文档写入阶段 19 当前目标、阶段范围和非目标。
3. 优先评估 QQ 互联网页登录、回调域名、开放平台资质，以及与 Supabase Auth 统一身份中心的桥接方式。
4. 保持阶段 16 邮箱验证码登录作为稳定兜底，保留阶段 17 微信入口和阶段 18 手机号入口的配置边界，不伪造未配置的真实登录能力。
5. 后续阶段按文档继续推进：阶段 20 账号绑定、阶段 21 国际化与文案治理。
6. 阶段完成后同步更新 `docs/技术栈分析.md`、`docs/中国用户登录方案文档.md` 和本文档，并使用 Git Bash 完整验证。
